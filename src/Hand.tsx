import React from 'react';
import CardList from './CardList';

const Hand = () => (
  <div className="Hand">
    <p>
      Hand
    </p>

    <CardList cards={[]} />
  </div>
);

export default Hand;
